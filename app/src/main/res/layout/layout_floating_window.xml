<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/window_root"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/bg_floating_window"
    android:orientation="vertical">

    <!-- Header bar -->
    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="38dp"
        android:background="@drawable/bg_floating_header"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingLeft="6dp"
        android:paddingRight="6dp">

        <!-- Tab Menu button -->
        <LinearLayout
            android:id="@+id/btn_tab_menu"
            android:layout_width="wrap_content"
            android:layout_height="26dp"
            android:background="@drawable/bg_tab_pill"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingLeft="8dp"
            android:paddingRight="10dp">

            <ImageView
                android:id="@+id/img_tab_icon"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@drawable/ic_tab_menu" />

            <TextView
                android:id="@+id/tv_tab_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="4dp"
                android:text="TAB"
                android:textColor="#FFFFFF"
                android:textSize="12sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- [+] Them tab game moi -->
        <TextView
            android:id="@+id/btn_add_tab"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:layout_marginStart="4dp"
            android:background="@drawable/bg_tab_pill"
            android:gravity="center"
            android:text="+"
            android:textColor="#00E5FF"
            android:textSize="18sp"
            android:textStyle="bold"
            android:clickable="true"
            android:focusable="true" />

        <!-- Move handle -->
        <LinearLayout
            android:id="@+id/move_handle"
            android:layout_width="0dp"
            android:layout_height="fill_parent"
            android:layout_weight="1"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:gravity="center"
            android:orientation="horizontal">

            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@drawable/ic_drag_handle" />

            <TextView
                android:id="@+id/window_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="4dp"
                android:text="J2ME Namod"
                android:textColor="@color/text_secondary"
                android:textSize="11sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- Hide Keyboard button -->
        <ImageView
            android:id="@+id/btn_hide_keyboard"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginRight="2dp"
            android:background="@drawable/bg_floating_icon_btn"
            android:padding="6dp"
            android:src="@drawable/ic_keyboard_hide"
            android:tint="@color/text_secondary"
            android:contentDescription="An ban phim" />

        <!-- Maximize button -->
        <ImageView
            android:id="@+id/maximize_window"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginRight="2dp"
            android:background="@drawable/bg_floating_icon_btn"
            android:padding="6dp"
            android:src="@android:drawable/ic_menu_zoom"
            android:tint="@color/text_secondary" />

        <!-- Minimize button -->
        <ImageView
            android:id="@+id/close_window"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@drawable/bg_floating_icon_btn"
            android:padding="6dp"
            android:src="@drawable/ic_minimize"
            android:tint="@color/text_secondary" />
    </LinearLayout>

    <!-- Tab chip row: [1][2][3]... de chuyen tab -->
    <HorizontalScrollView
        android:id="@+id/tab_chip_scroll"
        android:layout_width="fill_parent"
        android:layout_height="28dp"
        android:background="#0D000000"
        android:scrollbars="none"
        android:visibility="gone"
        android:paddingLeft="4dp"
        android:paddingRight="4dp">

        <LinearLayout
            android:id="@+id/tab_chip_row"
            android:layout_width="wrap_content"
            android:layout_height="fill_parent"
            android:gravity="center_vertical"
            android:orientation="horizontal" />
    </HorizontalScrollView>

    <!-- Game Screen & Overlay Container -->
    <FrameLayout
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/background">

        <FrameLayout
            android:id="@+id/game_container"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:background="@color/background" />

        <javax.microedition.lcdui.overlay.OverlayView
            android:id="@+id/vOverlay"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent" />

        <!-- Resize corner -->
        <ImageView
            android:id="@+id/resize_handle"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="bottom|end"
            android:background="@drawable/bg_floating_icon_btn"
            android:padding="4dp"
            android:src="@drawable/ic_resize_corner"
            android:tint="@color/text_secondary" />
    </FrameLayout>

    <!-- Bottom Command Bar -->
    <LinearLayout
        android:id="@+id/floating_command_bar"
        android:layout_width="fill_parent"
        android:layout_height="46dp"
        android:background="@color/background"
        android:gravity="center_vertical|end"
        android:orientation="horizontal"
        android:paddingLeft="8dp"
        android:paddingRight="8dp"
        android:visibility="gone" />

</LinearLayout>